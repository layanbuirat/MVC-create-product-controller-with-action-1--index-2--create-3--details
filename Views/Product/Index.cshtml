@model List<Product>

    <h2>Product List</h2>
    <ul>
        @foreach (var p in Model)
{
        <li>
            @p.Name - <a href="/Product/Details/@p.Id">Details</a>
        </li>
}
    </ul>

